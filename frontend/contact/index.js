import {NAME, ROUTE, FORM_ANCHOR} from './constants';
import Container from './Container';
import saga from './saga';

export default {
    NAME,
    ROUTE,
    Container,
    saga,
    FORM_ANCHOR,
};
