Packages which cannot be updated
================================

 * `bootstrap:3.3.7` -- completely redone, in sass no less
 * `react-bootstrap:0.31.5` -- breaks panels interface
 * `react-markdown:2.5.1` -- breaks meritory and non-meritory hack 
